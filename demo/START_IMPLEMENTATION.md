# Start Implementation

此檔作為交接觸發器，內容說明要交付給 Implementation Agent 的任務：

- 實作並擴充 `demo/` 的 API、路由、測試與文件。
- 啟動點：請從 [demo/app.py](demo/app.py) 開始。
- 執行測試：`pytest demo`。

若要開始實作，請執行相應的 Implementation Agent。此檔即為 "Start Implementation" 的顯示觸發器。
