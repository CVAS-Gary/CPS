# Developer Agent - Task Specification

## Objective
實現 `add(a, b)` 函式並建立單元測試。

## Requirements
1. 建立 Python 模組 `math_ops.py`，包含 `add(a, b)` 函式
2. 建立 `test_math_ops.py` 單元測試檔案
3. 遵循所有 Python 編碼規範和命名約定
4. 確保程式碼通過 black 格式化和 flake8 檢查

## Deliverable
完成後建立 `handoff/developer-handoff.md`，說明完成的工作。
